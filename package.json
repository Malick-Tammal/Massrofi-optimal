{
  "name": "massrofi-optimal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 4444",
    "build": "vite build",
    "preview": "vite preview --port 8888",
    "deploy": "npm run build && ntl deploy --prod --dir=dist",
    "deployPreview": "npm run build && ntl deploy --dir=dist"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "svgo": "^3.3.2",
    "vite": "^6.1.0",
    "vite-plugin-image-optimizer": "^1.1.8"
  },
  "dependencies": {
    "@fontsource/tajawal": "^5.1.1",
    "gsap": "^3.12.7",
    "unplugin-fonts": "^1.3.1"
  }
}
